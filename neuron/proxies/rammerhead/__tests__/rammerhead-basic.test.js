describe('Rammerhead Proxy', () => {
  it('loads proxy core logic (module exists)', () => {
    const init = require('../src/index.js');
    expect(init).toBeDefined();
  });
});
// ... existing code ... <no existing code, new file> ...
